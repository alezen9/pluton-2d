---
title: Sprocket Plate
category: Parametric
description: Gear-like plate with polar tooth generation, bolt circle arrays, and radial dimensions.
---

import ExampleTabs from "@views/examples/components/ExampleTabs.svelte";
import SprocketPlate from "./SprocketPlate.svelte";

<ExampleTabs>
  <SprocketPlate client:visible slot="scene" />
  <Fragment slot="code">
    ```typescript
      // I need to rewrite this one because currently it's quite messy, sorry :)
    ```

  </Fragment>
</ExampleTabs>
